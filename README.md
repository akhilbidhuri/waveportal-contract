# WavePortal made using Hardhat

Simple solidity contract to handle waves(his, heys, etc), with scripts to test and deploy.

```shell
npx hardhat accounts
npx hardhat compile
npx hardhat clean
npx hardhat test
npx hardhat node
node scripts/sample-script.js
npx hardhat help
```
